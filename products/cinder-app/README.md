# LEGACY Electron Wrapper — NOT the Main Cinder Product

The main Cinder product is the **AnythingLLM fork** at `../cinder-anythingllm/`.

This directory contains a legacy Electron desktop wrapper and build artifacts.
All active Cinder development happens in the fork. Do NOT create or modify
feature code here.

See `../cinder-anythingllm/CINDER-FORK-PLAN.md` for current status and architecture.
