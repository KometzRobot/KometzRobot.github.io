# Loop Handoff — Loop 8826 | 2026-05-04 ~01:15 MST

## What Happened This Session

### USB Copy — Status: IN PROGRESS (Linux rsync + model blob running)

**Windows**: COMPLETE. All 70,369 files transferred. 11GB on USB — correct (exFAT 128KB clusters × 70K files = expected overhead). Source was 1.7GB, disk usage 11GB is not corruption.

**Discovery**: Previous sessions left 3 competing rsync processes + 1 zombie zip-extraction process running simultaneously. Killed them all. Verified Windows was already done. Started clean finish-copy.sh for remaining steps.

**Currently running** (as of session end):
- `scripts/usb-finish-copy.sh` (PID 1350613) — Linux rsync in progress
- `/tmp/copy-blobs.py` (PID 1351079) — 4.4GB model blob copy (~11% at session end)
- `/tmp/usb-monitor-final.py` (PID 1353234) — new monitor, already sent Joel a correction email
- `/tmp/usb-finish.log` — Linux rsync output
- `/tmp/copy-blobs.log` — model blob copy output (check % progress)

**NOTE**: Old notify script (1294609) sent a false "copy failed" email to Joel. New monitor (1353234) sent correction explaining Windows is done and usable. Joel should disregard the failure email.

**Finish-copy.sh flow**: Linux rsync → Mac rsync (if >10GB free) → set DONE → notify emails Joel

**USB state at session end** (sdg = USB, usb2 = CINDER-BOOT, usb3 = CINDER-APP):
- Windows/: 11GB COMPLETE
- Linux/: rsync IN PROGRESS
- Mac/: pending (may fail if <10GB free after Linux)
- ollama/ollama.exe: 40MB Windows binary
- ollama/ollama: 42MB Linux binary
- ollama/lib/ollama/: CPU DLLs + cuda_v12 (2.2GB) + cuda_v13
- ollama/models/manifests/: cinder/latest manifest
- ollama/models/blobs/: 4.4GB blob IN PROGRESS
- CINDER-BOOT (usb2): all launchers verified

## What to Do on Next Wake

```bash
bash scripts/check-background-tasks.sh
```

If DONE: check `du -sh /media/usb3/Cinder/*`. Joel should have received notify email.
If still RUNNING: let it continue, check again next wake.
If processes dead + status still RUNNING: copy failed — restart finish-copy.sh manually.

## Cluster Math (reference)

exFAT 128KB clusters × 70K files = ~8.8GB overhead. So 1.7GB source → 11GB on USB is correct.
This applies to Linux and Mac builds too — each will be ~11GB on USB.
40GB partition: Windows(11) + Linux(11) + model(5) = 27GB. Mac(11) makes it 38GB — might fit.

## Other State

- Dev.to article published: calibration audit (Loop 8822/8823)
- Journal 804 written: "The Cluster Problem"
- System swap ~70% (cleanup freed 1.7GB from zombie process)
- Directives outstanding: Sampson Henchman (partial), Gumroad/Offline bundle (shelved)
