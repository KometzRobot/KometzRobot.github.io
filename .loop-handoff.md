# Loop Handoff — 2026-05-04 (Loop 8887)
**Cinder audit in progress — 3 more bugs fixed this session**

## What was fixed this session (loop 8886-8887)

### Bug 1: LivingRoom — Alerts card hidden when weather unavailable
- **File**: `products/cinder-anythingllm/frontend/src/pages/Cinder/LivingRoom/index.jsx`
- **Problem**: Alerts card was inside `data?.weather && (...)` — if weather API failed, the whole grid was hidden, users couldn't see alerts at all
- **Fix**: Decoupled weather card and alerts card. Weather now shows "not configured" placeholder if missing. Alerts always visible.

### Bug 2: TimeCapsules reconnect type — server rejected null unlockAt
- **File**: `products/cinder-anythingllm/server/endpoints/timeCapsules.js`
- **Problem**: Server validation required `unlockAt` for ALL capsule types, but "reconnect" type correctly sends `unlockAt: null`
- **Fix**: Only require `unlockAt` when `unlockType === "date"` (or undefined). Non-date types skip date validation.

### Bug 3: TimeCapsule model — `new Date(null)` = Invalid Date
- **File**: `products/cinder-anythingllm/server/models/cinderTimeCapsule.js`
- **Problem**: `seal()` always called `new Date(unlockAt)` — if `unlockAt` is null (reconnect type), creates Invalid Date, Prisma insert fails
- **Fix**: Changed to `unlockAt ? new Date(unlockAt) : null`

## Audit status: COMPLETE for all 10 Cinder pages
All custom Cinder pages have been audited for UX bugs this session + previous sessions:
- **Safety**: inline restore confirm ✓ (previous sessions)
- **Agents**: inline workspace delete confirm ✓ (previous sessions)
- **Journal**: inline entry delete confirm ✓ (loop 8883)
- **Dreams**: inline dream delete confirm ✓ (loop 8883)
- **Memory**: inline memory delete confirm ✓ (loop 8883)
- **TimeCapsules**: reconnect unlock type + server validation fix ✓ (this session)
- **Growth**: no bugs found
- **SkillTree**: no bugs found
- **Identity**: no bugs found (read-only)
- **LivingRoom**: alerts card decoupled from weather ✓ (this session)
- **Modelfile**: repeat_penalty 1.2, 10 speech rules, rebuilt ✓ (loop 8883)

## What's next for Cinder
- All frontend UX bugs resolved. Consider server-side hardening next: quests, nook, backup endpoints
- Test the full user flow end-to-end when possible
- Joel directive: "keep working on cinder and fixing bugs and errors in experience"

## Email
- Last check: 12:19 UTC (1hr cadence)
- Joel: "Keep working on cinder" + "limit to once an hour" for check-ins

---
*Written manually. Read this at wake for context.*
